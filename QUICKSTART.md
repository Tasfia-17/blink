# Blink - Quick Start Guide

## 🚀 Getting Started

```bash
# Install dependencies
npm install

# Start development server
npm run dev
```

Open http://localhost:5173 in your browser.

## ⌨️ Try These Actions

1. Press `Ctrl+Shift+B` (or `Cmd+Shift+B` on Mac) to open capture dialog
2. Type a note and press `Esc` to save
3. Press `Ctrl+Shift+H` to view history
4. Type `/` at the start of a note to see slash commands
5. Click "📌 Pin" to keep a note floating on screen

## 📦 Build for Production

```bash
npm run build
npm run preview
```

## 🎯 Next Steps

- Customize templates in `src/CaptureDialog.tsx`
- Adjust colors in `tailwind.config.js`
- Add more slash commands
- Export notes feature (coming soon)

---

**Built with Kendo UI for React + Tailwind CSS**
